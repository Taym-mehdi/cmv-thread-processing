# CMV Thread Processing

Project for preprocessing and analyzing thread-structured argumentative discussions from the r/ChangeMyView dataset.

## Goal
Reconstruct the original discussion thread structure from raw Reddit API data, preserve thread/comment IDs and timestamps, handle deleted comments, and compute dataset statistics for later constructiveness analysis.